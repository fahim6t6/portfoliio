<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Fahim – Portfolio</title>
<style>
  * { margin:0; padding:0; box-sizing:border-box; }
  body {
    font-family: Arial, sans-serif;
    background: #111;
    color: #f2f2f2;
    overflow-x: hidden;
    scroll-behavior: smooth;
    position: relative;
  }
  body::before {
    content:"";
    position:fixed; top:0; left:0; width:100%; height:100%;
    background: radial-gradient(#333 1px, transparent 1px);
    background-size:30px 30px;
    z-index:-2;
    animation: moveBg 60s linear infinite;
  }
  @keyframes moveBg { 0%{background-position:0 0;}100%{background-position:1000px 1000px;} }

  header { display:flex; justify-content:space-between; align-items:flex-start; padding:50px 80px; min-height:100vh; }
  .hero-text { display:flex; flex-direction:column; gap:20px; max-width:60%; animation: fadeInLeft 1s ease forwards; opacity:0; }
  .hero-text h1 { font-size:2.9rem; display:inline-flex; align-items:flex-end; }
  .orange-dot{width:8px; height:8px; background:#ff6600; border-radius:50%; display:inline-block; margin-left:5px; margin-bottom:12px; animation:pulse 1.5s infinite;}
  .hero-text h2{font-size:3.2rem; display:flex; align-items:center; gap:15px;}
  .orange-line{height:4px;width:90px;background:#ff6600;border-radius:5px;}
  .hero-text h3{font-size:3.8rem;color:#ccc;}
  @keyframes pulse{0%{transform:scale(1);}50%{transform:scale(1.3);}100%{transform:scale(1);} }
  @keyframes fadeInLeft{0%{opacity:0;transform:translateX(-50px);}100%{opacity:1;transform:translateX(0);} }

  .button-row{display:flex; gap:2cm; margin-top:30px; align-items:center;}
  .button-row button{padding:15px 50px; border-radius:50px; border:2px solid #ff6600; background:transparent; color:#ff6600; font-size:1.5rem; cursor:pointer; transition:all .3s; max-width:200px;}
  .button-row button:hover{background:#ff6600;color:#111;}
  .dropdown{position:relative;}
  .dropdown-content{display:none; position:absolute; top:120%; left:0; background:#222; border:1px solid #ff6600; border-radius:8px; min-width:150px; z-index:10; flex-direction:column;}
  .dropdown-content a{padding:10px 15px; color:#ff6600; text-decoration:none; display:block;}
  .dropdown-content a:hover{background:#ff6600;color:#111;}
  .dropdown.show .dropdown-content{display:flex;}

  .orange-divider {
    width: 43.5cm;
    height: 4px;
    background: rgba(255, 102, 0, 0.7);
    margin-top: 40px;
    margin-left: -1.4cm;
  }

  .profile-pic {
    width: 300px;
    height: 300px;
    border-radius: 50%;
    border: 0px solid #ff6600;
    overflow: hidden;
    margin-top: 0.6cm;
  }
  .profile-pic img{width:100%; height:100%; object-fit:cover;}
  @keyframes fadeInRight{0%{opacity:0;transform:translateX(50px);}100%{opacity:1;transform:translateX(0);} }

  /* New sections layout with larger columns */
  .sections-container {
    display: flex;
    flex-direction: column;
    margin-top: 40px;
    width: 100%;
    gap: 60px; /* Increased gap for better separation */
  }

  /* Top row (About Me and Education) */
  .top-row {
    display: flex;
    justify-content: space-between;
    gap: 60px; /* Increased gap */
  }

  /* Bottom row (Skills) */
  .bottom-row {
    display: flex;
    justify-content: center;
    gap: 60px; /* Increased gap */
  }

  /* About Me section - larger and more prominent */
  .about-me {
    flex: 1.2; /* Increased flex value for larger column */
    text-align: left;
    line-height: 1.7; /* Increased line height for better readability */
    padding: 25px; /* Added padding for better spacing */
    background: rgba(30, 30, 30, 0.5); /* Subtle background */
    border-radius: 12px; /* Rounded corners */
    border: 1px solid rgba(255, 102, 0, 0.3); /* Subtle border */
  }
  .about-me h2 {
    color: #ff6600;
    font-size: 2.2rem; /* Slightly larger font */
    margin-bottom: 20px; /* Increased margin */
    border-bottom: 2px solid #ff6600; /* Underline effect */
    padding-bottom: 10px;
  }
  .about-me p {
    color: #ddd;
    font-size: 1.3rem; /* Slightly larger font */
  }

  /* Education section - larger and more prominent */
  .education {
    flex: 1; /* Keep equal with About Me */
    padding: 25px; /* Added padding for better spacing */
    background: rgba(30, 30, 30, 0.5); /* Subtle background */
    border-radius: 12px; /* Rounded corners */
    border: 1px solid rgba(255, 102, 0, 0.3); /* Subtle border */
  }
  .education h2 {
    color: #ff6600;
    font-size: 2.2rem; /* Slightly larger font */
    margin-bottom: 20px; /* Increased margin */
    border-bottom: 2px solid #ff6600; /* Underline effect */
    padding-bottom: 10px;
  }
  .education-list {
    list-style-type: none;
    text-align: left;
  }
  .education-list li {
    color: #ddd;
    font-size: 1.3rem; /* Slightly larger font */
    margin-bottom: 15px; /* Increased margin */
    line-height: 1.7; /* Increased line height */
    padding-left: 20px;
    position: relative;
  }
  .education-list li:before {
    content: "•";
    color: #ff6600;
    position: absolute;
    left: 0;
    font-size: 1.5rem;
  }

  /* Skills section - larger and more prominent */
  .skills {
    flex: 1.2; /* Increased flex value for larger column */
    padding: 25px; /* Added padding for better spacing */
    background: rgba(30, 30, 30, 0.5); /* Subtle background */
    border-radius: 12px; /* Rounded corners */
    border: 1px solid rgba(255, 102, 0, 0.3); /* Subtle border */
  }
  .skills h2 {
    color: #ff6600;
    font-size: 2.2rem; /* Slightly larger font */
    margin-bottom: 20px; /* Increased margin */
    border-bottom: 2px solid #ff6600; /* Underline effect */
    padding-bottom: 10px;
  }
  .skills-box {
    border: 2px solid #ff6600;
    border-radius: 10px;
    padding: 25px; /* Increased padding */
    display: flex;
    flex-wrap: wrap;
    gap: 20px; /* Increased gap */
    background: rgba(20, 20, 20, 0.7);
  }
  .skill-item {
    background: rgba(255, 102, 0, 0.2);
    color: #ff6600;
    padding: 12px 25px; /* Increased padding */
    border-radius: 20px;
    font-size: 1.2rem; /* Slightly larger font */
    border: 1px solid #ff6600;
    transition: all 0.3s ease;
  }
  .skill-item:hover {
    background: rgba(255, 102, 0, 0.4);
    transform: translateY(-3px);
  }

  /* Contact Icons Section - Updated to right side */
  .contact-icons {
    display: flex;
    justify-content: flex-end; /* Changed from center to flex-end */
    gap: 40px;
    margin-top: 40px;
    margin-bottom: 20px;
    margin-right: 20px; /* Added right margin for better positioning */
  }
  .icon-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    cursor: pointer;
    transition: transform 0.3s ease;
  }
  .icon-container:hover {
    transform: translateY(-5px);
  }
  .contact-icon {
    width: 60px;
    height: 60px;
    background: rgba(255, 102, 0, 0.2);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2px solid #ff6600;
    margin-bottom: 10px;
    font-size: 28px;
    color: #ff6600;
  }
  .icon-label {
    color: #ddd;
    font-size: 1rem;
  }

  /* Projects grid */
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 60px 90px;
    margin-top: 40px;
    margin-bottom: 4cm;
    width: 100%;
    max-width: 1600px;
    margin-left: auto;
    margin-right: auto;
  }

  .project-item { display: flex; flex-direction: column; align-items: center; }
  .project-box {
    width: 100%;
    height: 220px;
    border: 3px solid #ff6600;
    border-radius: 14px;
    overflow: hidden;
    cursor: pointer;
    transition: transform .22s, box-shadow .22s;
    background: #0e0e0e;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .project-box img { width:100%; height:100%; object-fit: cover; display: block; }
  .project-box:hover { transform: scale(1.04); box-shadow: 0 0 30px rgba(255,102,0,0.45); }
  .project-title { margin-top: 12px; color: #ff6600; font-size: 1rem; text-align: center; }

  /* Custom placement */
  .projects-grid > .project-item:nth-child(1),
  .projects-grid > .project-item:nth-child(4),
  .projects-grid > .project-item:nth-child(7) { grid-column: 1; }
  .projects-grid > .project-item:nth-child(2),
  .projects-grid > .project-item:nth-child(5) { grid-column: 2; }
  .projects-grid > .project-item:nth-child(3),
  .projects-grid > .project-item:nth-child(6) { grid-column: 3; }

  .project-detail{max-width:900px; margin:0 auto 4cm auto; text-align:left;}

  /* ✅ Fixed video size - UPDATED FOR GOOGLE DRIVE */
  .video-wrapper {
    display:flex;
    justify-content:center;
    margin-bottom:20px;
  }
  .drive-player {
    width:800px;
    height:420px;
    border-radius:8px;
    background:#000;
    overflow:hidden;
    border: none;
  }
  .drive-fallback { 
    width:800px; 
    height:420px; 
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background: #222;
    border-radius: 8px;
    color: #ddd;
  }
  .drive-fallback p { 
    color:#ddd; 
    margin-top:15px; 
    font-size:1rem; 
    text-align: center;
  }

  .capsule-btn{border:2px solid #ff6600; background:transparent; color:#ff6600; padding:10px 20px; border-radius:50px; font-size:1rem; cursor:pointer;}
  .capsule-btn:hover{background:#ff6600;color:#111;}
  .fixed-back{position:fixed; top:20px; right:20px; z-index:2000;}
  
  /* Project points styling */
  .project-points {
    margin-top: 20px;
    padding-left: 20px;
  }
  .project-points li {
    margin-bottom: 10px;
    color: #ddd;
    font-size: 1.1rem;
    line-height: 1.5;
  }
</style>
</head>
<body>

<header>
  <div class="hero-text">
    <h1>Hello<span class="orange-dot"></span></h1>
    <h2><span class="orange-line"></span> I'm Fahim</h2>
    <h3>Game Developer</h3>

    <div class="button-row">
      <button onclick="loadProject()">Project</button>
      <div class="dropdown">
        <button onclick="toggleDropdown()">CV</button>
        <div class="dropdown-content">
          <a href="https://github.com/fahim6t6/Fahim/blob/main/Fahim(cv).pdf" target="_blank">View</a>
          <a href="https://github.com/fahim6t6/Fahim/blob/main/Fahim(cv).pdf?raw=true" download>Download</a>
        </div>
      </div>
    </div>
    <div class="orange-divider"></div>

    <div class="sections-container">
      <div class="top-row">
        <div class="about-me">
          <h2>About Me</h2>
          <p>
            I am a tech enthusiast with experience in managing the full game development lifecycle—
            from concept and prototyping to testing and deployment—primarily using the Unity engine. 
            Passionate about building immersive, interactive experiences with a strong focus on performance 
            and player engagement. Eager to join a creative team to contribute fresh ideas, grow technically, 
            and develop high-quality games for PC and mobile platforms.
          </p>
        </div>

        <div class="education">
          <h2>Education</h2>
          <ul class="education-list">
            <li>Game Developer Course</li>
            <li>BSc Computer Science</li>
            <li>Computer Science (Higher Secondary)</li>
          </ul>
        </div>
      </div>

      <div class="bottom-row">
        <div class="skills">
          <h2>Skills</h2>
          <div class="skills-box">
            <span class="skill-item">Unity 3D</span>
            <span class="skill-item">Unity 2D</span>
            <span class="skill-item">Unity Animation</span>
            <span class="skill-item">C#</span>
            <span class="skill-item">Python</span>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Contact Icons Section -->
    <div class="contact-icons">
      <div class="icon-container" onclick="openGmail()">
        <div class="contact-icon">✉️</div>
        <span class="icon-label">Gmail</span>
      </div>
      <div class="icon-container" onclick="openIndeed()">
        <div class="contact-icon">💼</div>
        <span class="icon-label">Indeed</span>
      </div>
      <div class="icon-container" onclick="openLinkedIn()">
        <div class="contact-icon">🔗</div>
        <span class="icon-label">LinkedIn</span>
      </div>
    </div>

  </div>

  <div class="profile-pic">
    <img src="https://raw.githubusercontent.com/fahim6t6/Fahim/main/IMG_7607.jpg" alt="Fahim Profile">
  </div>
</header>

<script>
(function () {
  function $ (sel) { return document.querySelector(sel); }

  function toggleDropdown(){
    const dd = $('.dropdown');
    if(!dd) return;
    dd.classList.toggle('show');
  }
  window.toggleDropdown = toggleDropdown;

  // Contact functionality
  function openGmail() {
    window.open('mailto:fahim6t6@gmail.com');
  }
  
  function openIndeed() {
    window.open('https://profile.indeed.com/p/muhammedf-xfxqr3j', '_blank');
  }
  
  function openLinkedIn() {
    window.open('https://www.linkedin.com/in/muhammed-fahim-23609b36a?utm_source=share_via&utm_content=profile&utm_medium=member_android', '_blank');
  }
  
  window.openGmail = openGmail;
  window.openIndeed = openIndeed;
  window.openLinkedIn = openLinkedIn;

  // Google Drive video functionality
  function extractGoogleDriveFileId(url) {
    if (!url) return null;
    
    const patterns = [
      /\/d\/([^\/]+)/,
      /\/file\/d\/([^\/]+)/,
      /id=([^&]+)/,
      /([a-zA-Z0-9_-]{25,})/
    ];
    
    for (const pattern of patterns) {
      const match = url.match(pattern);
      if (match && match[1]) {
        return match[1];
      }
    }
    
    console.error('Could not extract Google Drive file ID from URL:', url);
    return null;
  }

  function createGoogleDriveEmbed(driveLink) {
    const fileId = extractGoogleDriveFileId(driveLink);
    if (!fileId) {
      return `
        <div class="drive-fallback">
          <p>Video not available</p>
          <p><a href="${driveLink}" target="_blank" style="color:#ff6600">Open in Google Drive</a></p>
        </div>
      `;
    }
    
    const embedUrl = `https://drive.google.com/file/d/${fileId}/preview`;
    
    return `
      <iframe 
        src="${embedUrl}"
        class="drive-player"
        frameborder="0"
        allow="autoplay; encrypted-media; fullscreen"
        allowfullscreen
        title="Google Drive Video Player">
      </iframe>
    `;
  }

  function ensureBackButton(){
    let btn = document.getElementById('backBtn');
    if(!btn){
      btn = document.createElement('button');
      btn.id = 'backBtn';
      btn.className = 'capsule-btn fixed-back';
      document.body.appendChild(btn);
    }
    // Make sure the button is visible
    btn.style.display = 'block';
    return btn;
  }
  window.ensureBackButton = ensureBackButton;

  // Store the original home content
  const originalHomeContent = document.querySelector('.hero-text').innerHTML;

  // Google Drive video links
  const driveVideos = {
    bowling: 'https://drive.google.com/file/d/1o5zCCTqqNYa0JaZFbKNG78p723zTrdQP/view?usp=drivesdk',
    endless: 'https://drive.google.com/file/d/1vG7jjHGlXUtp-bGqt6rvjRCAY1lahARB/view?usp=drivesdk',
    tank: 'https://drive.google.com/file/d/10ilh2Qev35HVR6l61sBsPoey-Px3-qdc/view?usp=drivesdk',
    skating: 'https://drive.google.com/file/d/1v135bDtjt62H9B79UqAb0RkVn0nc8J7i/view?usp=drivesdk',
    pizza: 'https://drive.google.com/file/d/12YgrkZVpOlT6GkQIXww3CTCwe9tFMVTW/view?usp=drivesdk',
    jetcoin: 'https://drive.google.com/file/d/1Uv_9mvrXKqnWzMDVKH-3CHSj3R9LxjPw/view?usp=drivesdk',
    jetpark: 'https://drive.google.com/file/d/1ynY8w6l56bwi8N-xQgBz5X5vKJ6fgiN9/view?usp=drivesdk'
  };

  window.loadProject = function(){
    const heroText = $('.hero-text');
    const profilePic = $('.profile-pic');
    if(profilePic) profilePic.style.display='none';
    if(!heroText) return;

    heroText.innerHTML = `<div class="projects-grid">
      <div class="project-item"><div class="project-box" onclick="openProject('bowling')"><img src="https://raw.githubusercontent.com/fahim6t6/Fahim/refs/heads/main/Bowling.jpg" alt="Bowling"></div><div class="project-title">Bowling</div></div>
      <div class="project-item"><div class="project-box" onclick="openProject('endless')"><img src="https://raw.githubusercontent.com/fahim6t6/Fahim/refs/heads/main/Endless.png" alt="Endless Runner"></div><div class="project-title">Endless Runner</div></div>
      <div class="project-item"><div class="project-box" onclick="openProject('tank')"><img src="https://raw.githubusercontent.com/fahim6t6/Fahim/refs/heads/main/Tank%20vs%20Tank.jpg" alt="Tank"></div><div class="project-title">Tank vs Tank</div></div>
      <div class="project-item"><div class="project-box" onclick="openProject('skating')"><img src="https://raw.githubusercontent.com/fahim6t6/Fahim/refs/heads/main/Skating.jpg" alt="Skating"></div><div class="project-title">Snow Skating</div></div>
      <div class="project-item"><div class="project-box" onclick="openProject('pizza')"><img src="https://raw.githubusercontent.com/fahim6t6/Fahim/refs/heads/main/Pizza.jpg" alt="Pizza"></div><div class="project-title">Pizza Delivery</div></div>
      <div class="project-item"><div class="project-box" onclick="openProject('jetcoin')"><img src="https://raw.githubusercontent.com/fahim6t6/Fahim/refs/heads/main/Space%20jet.jpg" alt="Jet Coin"></div><div class="project-title">Jet Coin Collector</div></div>
      <div class="project-item"><div class="project-box" onclick="openProject('jetpark')"><img src="https://raw.githubusercontent.com/fahim6t6/Fahim/refs/heads/main/Jet%20parking.jpg" alt="Jet Parking"></div><div class="project-title">Jet Parking</div></div>
    </div>`;

    const backBtn = ensureBackButton();
    backBtn.innerText = 'Back';
    backBtn.style.background = 'transparent';
    backBtn.style.color = '#ff6600';
    backBtn.onclick = function(){
      backBtn.style.background = '#ff6600';
      backBtn.style.color = '#111';
      setTimeout(()=> backToHome(), 180);
    };
  };

  window.openProject = function(name){
    const heroText = $('.hero-text');
    const profilePic = $('.profile-pic');
    if(profilePic) profilePic.style.display='none';
    if(!heroText) return;

    const driveLink = driveVideos[name];
    let content = `<div class="project-detail">
      <div class="video-wrapper">`;
    
    if(driveLink){
      content += createGoogleDriveEmbed(driveLink);
    } else {
      content += `<div class="drive-fallback"><p>Video not available</p></div>`;
    }
    
    content += `</div>
      <ul class="project-points">`;

    if(name === 'bowling'){
      content += `<li>Implementing realistic physics for accurate pin and ball interactions</li>
        <li>Scripted realistic ball dynamics using Unity's Rigidbody and physics engine for authentic gameplay feel</li>
        <li>Gaming controls assigned to A (ball move to left), D (ball move to right), Space (shoot ball)</li>
        <li>Added UI/UX highscore button to show highest score</li>
        <li>Integrated music and sound effects for rolling ball and pin fall</li>`;
    } else if(name === 'endless'){
      content += `<li>Implemented collectible coins placed along the running path</li>
        <li>Player collects coins on collision, increasing the score</li>
        <li>Collision mechanics for interacting with coins, obstacles, and environment</li>
        <li>Background music & sound effects for coin collection and collisions</li>
        <li>Running & fall animations for immersive gameplay</li>
        <li>Dynamically spawning ground & environment tiles for endless play</li>`;
    } else if(name === 'tank'){
      content += `<li>Focus on player vs enemy combat in 3D environment</li>
        <li>Enemy AI: patrol, detection, and attack behaviors</li>
        <li>Integrated a dynamic health bar Ul for both player</li>
        <li>Add sound effects for firing & tank movements
        <li>Firing mechanics with projectile physics and collision detection</li>`;
    } else if(name === 'skating'){
      content += `<li>Navigate snow skate character across icy terrain, avoiding hazards</li>
        <li>Surface Effector 2D used for slippery sliding physics</li>
        <li>Particle effect when the player win,hits an obstacle or lands a jump</li>
        <li>Sliding sound on snow, a crunchy collision sound for impacts, and ambient music</li>
        <li>Sprite Shape Controller for smooth hills, dips, curves</li>`;
    } else if(name === 'pizza'){
      content += `<li>User-friendly gameplay and appealing graphics</li>
        <li>WASD controls for car movement</li>
        <li>Designed roads, pond, river, trees and houses</li>
        <li>Pizza pick-up changes car color, resets after delivery</li>`;
    } else if(name === 'jetcoin'){
      content += `<li>Player collects coins while avoiding obstacles</li>
        <li>Responsive controls for dodging obstacles and collecting coins</li>
        <li>Detects when the jet collides with coins (to collect) or obstacles (to end game)</li>
        <li>Endless background generated through spawning and looping space tiles</li>
        <li>Asteroids, coins, and power-ups spawn randomly for variety</li>
        <li>Sound effects for coin collection, collisions</li>`;
    } else if(name === 'jetpark'){
      content += `<li>Basic designed and developed game mechanics, level design</li>
        <li>Pilot a jet from one island to another and successfully land (park) it on a target pad</li>
        <li>5 levels of increasing difficulty, featuring narrow paths, static obstacles, and moving creatures</li>
        <li>Use W (forward), A (rotate left), S (brake/reverse), D (rotate right), and Space (up)</li>
        <li>A dynamic particle effect triggers upon each crash to emphasize impact</li>
        <li>Navigate the challenging environment,avoid all hazards,and land safely to complete each level.</li>`;
    }

    content += `</ul></div>`;
    heroText.innerHTML = content;
    
    // Update back button to return to projects grid
    const backBtn = ensureBackButton();
    backBtn.innerText = 'Back';
    backBtn.style.background = 'transparent';
    backBtn.style.color = '#ff6600';
    backBtn.onclick = function(){
      backBtn.style.background = '#ff6600';
      backBtn.style.color = '#111';
      setTimeout(()=> loadProject(), 180);
    };
  };

  function backToHome(){
    const heroText = $('.hero-text');
    const profilePic = $('.profile-pic');
    if(profilePic) profilePic.style.display='block';
    
    // Restore the original home content
    heroText.innerHTML = originalHomeContent;
    
    // Remove the back button when on home page
    const backBtn = document.getElementById('backBtn');
    if(backBtn) backBtn.style.display = 'none';
  }

  // Initialize - hide back button on page load
  document.addEventListener('DOMContentLoaded', function() {
    const backBtn = document.getElementById('backBtn');
    if(backBtn) backBtn.style.display = 'none';
  });

})();
</script>
</body>
</html>
